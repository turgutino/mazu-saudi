# 任务计划：沙特区域多源气象数据裁剪脚本

## 目标

为 `/Volumes/E/气象数据` 下的全量数据设计并实现一个可扩展脚本，批量裁剪子数据集 1-4 的沙特区域数据，并支持子数据集 10-11 的沙特区域提取。

## 当前状态

- [x] 建立规划文件
- [x] 轻量探查数据根目录和子目录结构
- [x] 抽样识别 DS1-DS4、DS10、DS11 的文件格式、命名规律和维度信息
- [x] 阅读现有 `saudi_data_extract.py` 并识别可复用逻辑
- [x] 设计新版脚本的模块结构、命令行参数和输出布局
- [x] 规划测试策略与小样本验证方式
- [x] 等用户确认后进入实现
- [x] 实现路径工具、数据发现、网格裁剪、DS10 HDF5、DS11 轨迹过滤和批处理 CLI
- [x] 使用 conda `ml` 环境完成单元测试与真实数据 smoke test
- [x] 建立 `analysis/` 数据洞察子目录，生成初步可视化和图文分析报告
- [x] 撰写极端事件指标方法与分布洞察报告
- [x] 理解 `reference_code/` 中的三组极端天气预测/服务工程
- [x] 理解 `turgutino/mazu-saudi-warning` 的架构与方法
- [x] 形成工程对比、可复用资产、风险与后续研究入口的初步结论
- [x] 理解新增 `吴诗华-刘贤君-报告+代码+PPT` 工程并纳入横向对比
- [x] 基于五个参考工程重新设计自有竞赛与论文双轨框架
- [x] 对 HAMF 创新假设进行文献查新并评估计算机论文可发表性
- [x] 完成干旱区短历时极端降水英文论文初稿与发表路线包
- [x] 形成预测方法与 AI4Science 两套独立论文初稿和试验计划

## 阶段划分

### 阶段 1：数据目录轻量探查

只查看目录、文件名、扩展名、少量文件头或元数据。禁止递归读取大文件内容，避免对 2T 数据做重 IO。

### 阶段 2：现有脚本分析

确认当前脚本支持范围、硬编码路径、单月/单日限制和输出方式。

### 阶段 3：新版脚本设计

形成支持 DS1-DS4、DS10、DS11 的批处理设计，包括格式适配器、经纬度裁剪、输出命名、断点续跑、dry-run、日志与错误处理。

### 阶段 4：实现计划

列出文件修改、测试文件、样例命令、提交边界和验证命令。

实现计划已保存到 `docs/plans/2026-07-03-saudi-data-extraction.md`。

### 阶段 5：脚本实现

已在 `codex-saudi-data-extraction` 分支实现新版 `saudi_data_extract.py`，并新增 `tests/test_saudi_data_extract.py`。

### 阶段 6：真实数据烟测

已对 `/Volumes/E/气象数据` 执行轻量 dry-run，并对 DS10、DS11 运行一文件 smoke test。

### 阶段 7：数据洞察分析与图文报告

目标是在仓库中新增可复跑的 `analysis/` 子目录，面向
`/Volumes/E/气象数据/saudi_region_output` 已裁剪数据生成：

- 数据可用性与覆盖范围摘要。
- 月尺度/日尺度关键气象指标统计表。
- 初步可视化图件。
- Markdown 图文分析报告。
- 聚焦测试，覆盖文件发现、数值统计、DS10 非数值字段跳过和报告生成。

已完成，输出位于 `analysis/`：

- `analysis/analyze_saudi_region_output.py`
- `analysis/summary.json`
- `analysis/saudi_data_insights_report.md`
- `analysis/figures/*.png`

### 阶段 8：极端事件指标方法与分布洞察报告

目标是基于 `compute_indicators.py` 和
`/Volumes/E/气象数据/saudi_region_output/indicators` 的真实指标输出，撰写
Markdown 报告，说明：

- 每个指标的物理含义和服务的灾害场景。
- 输入变量、计算方法、单位与解释方式。
- 计算后在 2025 年沙特区域数据中的覆盖情况和分布洞察。
- 已知缺测、填充值、基线不足或垂直廓线不足导致的限制。

已完成，输出位于 `docs/saudi_extreme_event_indicator_report.md`。

### 阶段 9：指标可信度质检

目标是对后续分析将使用的 `indicator` 输出做可复验质检，重点确认：

- 指标网格确实落在沙特区域裁剪范围内。
- 指标文件和上游 `ds1/ds2/ds4/ds10_daily` 日期、空间坐标一致。
- 关键指标公式可从上游变量复算并在容差内匹配。
- 缺测、填充值、网格对齐和变量覆盖率问题被明确标记。

状态：已完成初轮质检；空间裁剪和关键公式抽样复算通过，但发现热湿填充值污染和 `flash_flood_risk` 未实际使用三维 CAPE 的问题，后续分析前建议先修复并重算。

### 阶段 10：指标质控修复与重算

目标是修复阶段 9 暴露的指标质量问题：

- 计算热湿派生指标前清理 GRIB 缺测填充值和明显非物理值。
- 将带额外层维度的 CAPE 等稳定度变量折算到二维水平网格。
- 保持 DS10 缺测口径明确：日总量可用 DS2 日累计降水替代参考，短时强降水不做伪替代。

状态：已完成。正式指标输出已重算，QA 确认热湿异常值清除、CAPE 转为二维、`flash_flood_risk` 全年二维且可纳入 CAPE 项。

### 阶段 11：基于 DS8 气候态的异常类预警指标

目标是检查数据 8 的性质，并在指标脚本中补充更适合预警业务解释的异常类指标：

- 确认 DS8 是否为 1991-2020 多年气候态数据，以及它是站点表还是格点数据。
- 在可支撑的数据范围内新增降水距平、气温距平和热浪连续日数。
- 明确 SPI 和 500 hPa 高度距平的数据需求；若当前 DS8 不足以支撑，禁止伪计算，并在 NetCDF 属性和报告中说明缺口。
- 用测试覆盖 DS8 表解析、站点气候态映射、距平计算和热浪连续日数回写。

状态：已完成。已确认 DS8 是 1991-2020 全球站点气候态表，包含 PRE/TAVG/TMAX/TMIN 的逐日和逐月 normals；不是格点气候态。沙特 bbox 内逐日 PRE 约 7 个有效站点、逐日 TMAX 约 11 个有效站点、逐日 TAVG 约 45 个有效站点，TMIN 在 bbox 内没有有效站点。脚本已新增 DS8 站点气候态映射后的降水距平、TAVG/TMAX 温度距平、热浪日标志和热浪持续天数，并在属性中明确标记 SPI 与 500 hPa 高度距平未计算原因；正式指标目录已重算。

### 阶段 12：参考工程逆向理解与横向对比

目标是在不修改参考代码的前提下，理解：

- `reference_code/` 中三组本地工程的任务定义、数据入口、指标/模型、评估、Agent 与部署链路。
- GitHub 项目 `turgutino/mazu-saudi-warning` 的同类结构。
- 各工程的真实完成度、可复用资产、技术债和可能的评估泄漏/科研有效性风险。
- 当前仓库与参考工程的关系，为下一步自有竞赛+论文方法设计确定起点。

状态：已完成首轮只读理解。详细对比见 `docs/reference_projects_review.md`；未重跑参考工程的大规模训练，所有自报指标仍需在自有评估协议下复现。

### 阶段 13：新增参考工程补充对比

目标是对 `reference_code/吴诗华-刘贤君-报告+代码+PPT` 进行同口径只读分析：

- 核对报告/PPT 声称与实际代码、数据、模型产物的一致性。
- 审计任务定义、标签来源、时空分割、阈值选择、评估指标和 Agent/服务链路。
- 与已分析的四个工程比较方法创新、真实完成度、可复用资产和科研风险。
- 更新 `docs/reference_projects_review.md` 的总体地图和自有方法结论。

状态：已完成。已核对 PDF、PPT、训练/推理入口、两代模型产物、Agent/KG/案例链和服务结构，并更新总对比报告。

### 阶段 14：自有预测框架重新设计

目标是形成一个不直接拼接参考代码、同时服务竞赛展示和论文研究的自有框架：

- 明确预测对象、预报时效、空间单元、标签层级和核心领域术语。
- 分离数据事实、标签构建、训练评估、在线预报、风险决策和 Agent 服务边界。
- 按灾种设计不同的时空机制，并建立无泄漏的训练/验证/测试协议。
- 设计可渐进实现的仓库结构、模块接口、实验矩阵、里程碑和验收标准。
- 记录难以逆转的架构决策，更新领域词汇表和总体设计文档。

状态：已完成。领域词汇见 `CONTEXT.md`，总体方案见 `docs/own_warning_framework.md`，关键边界已记录在 `docs/adr/0001-0004`。

### 阶段 15：创新假设文献查新

目标是以计算机论文审稿口径检验 HAMF 方案是否具有可发表的新颖性：

- 检索灾种感知/任务感知空间融合、多尺度极值保持、动态物理图和 mixture-of-experts 的相近工作。
- 检索缺测感知多模态融合、弱监督灾害标签、概率校准和天气过程级评估的相近工作。
- 区分已有技术、可形成的新组合、尚需实验验证的创新假设和不应宣称的“伪创新”。
- 给出适合计算机方向的技术突破点、最小论文问题、实验与消融要求、投稿层级判断。
- 形成带一手论文链接的查新报告，并据此修订自有框架的创新表述。

状态：已完成。查新报告见 `docs/hamf_novelty_review.md`；原 HAMF 被重新定位为系统框架与强基线，方法候选升级为以机制适用性先验、物理反事实约束和 OOD 可靠性为核心的 MCR-Hazard。

### 阶段 16：Nature Communications 级数据集路线设计

目标是从高影响综合期刊的证据标准倒推数据建设，而不是只扩充变量：

- 核对现有 2025 沙特指标底座与论文所需多年、独立真值、高频和跨区域数据之间的缺口。
- 检索可复用的一手再分析、卫星、雷达/站点、地形水文、沙尘与灾情/告警数据源及其时间空间能力。
- 设计可公开发布的数据产品、事件目录、标签层级、训练/验证/测试切分和 OOD 基准。
- 区分 Nature Communications 研究论文与 Scientific Data 数据论文所需贡献，给出主论文问题、最低可行范围和高风险依赖。
- 形成数据集蓝图、分阶段采集计划、质量控制和发布清单，并据此更新论文路线。

状态：已完成。数据与论文蓝图见 `docs/nature_communications_dataset_blueprint.md`；主线收敛为多区域干旱 Wadi 山洪事件数据、独立影响证据、临时河道图和真实 reforecast，热浪作为有健康数据时的增强路线。

### 阶段 17：干旱区短历时极端降水论文初稿

目标是在无法获得可靠山洪真值后，将第一篇论文主线收敛为“干旱区短历时极端降水的跨区域可靠预测”，并形成：

- Nature Communications 风格的英文结果就绪稿，未知数字全部使用显式 `TODO-RESULT-*` 占位符。
- 可复现的数据定义、MCR-Precip 方法、跨区域切分、基线、消融、统计检验和停止条件。
- Supplementary Methods、参考文献库、claim–evidence 矩阵、图表清单、审稿风险清单和发表工作计划。
- 自动合同检查，验证标题、摘要、必需章节、占位符和内部链接。

状态：已完成。论文包位于 `docs/manuscript/`，包含英文主稿、Supplementary Methods、BibTeX 参考文献、claim–evidence 矩阵、图表计划、审稿风险和发表路线。合同测试与本仓库完整适用测试通过；多年数据下载、训练和结果占位符替换仍是后续证据生产工作。

### 阶段 18：双论文组合与 AI4Science 初稿

目标是把同一数据底座上的两条研究路线明确分开：

- 论文 A 保留为 MCR-Precip 计算机方法/可靠预测论文，完整保留已有初稿和 Gate 0–5 试验计划。
- 论文 B 新建为 AI4Science 论文，以“跨尺度机制类型如何控制干旱区小时降水极值，并解释数值预报盲区”为科学主题。
- AI 在论文 B 中只是事件表征、共识机制发现和配对对照的工具，不把新网络结构作为主要结论。
- 建立双论文贡献边界、共享数据规则和先后顺序，避免两篇切割同一主张或重复使用未独立的测试证据。
- 新增自动合同测试，检查 AI4Science 初稿的标题/摘要、科学章节、结果占位符、非因果边界和双线索链接。

状态：已完成。论文 A 保留在 `docs/manuscript/`；论文 B 位于 `docs/ai4science_manuscript/`，包含英文初稿、Experiment 0–6 试验计划、claim–evidence 矩阵和 BibTeX 文献库。`docs/paper_portfolio.md` 定义了两篇论文的共享数据、非重叠贡献和建议执行顺序。聚焦合同与本仓库完整适用测试通过。

## 约束

- 原始数据目录超过 2T，只做轻量探查。
- 不提交原始数据或输出数据。
- 每个代码变更需要配套测试。
- 每个完成的代码或文档变更需要单独提交。
- 避免把用户已有未跟踪文件混入提交。
- 分析脚本只读取已裁剪的沙特区域输出，不扫描或复制全量原始数据。

## 错误记录

| 错误 | 尝试 | 处理 |
|---|---:|---|
| 直接打开 GitHub 仓库 URL 返回 `Cache miss` | 1 | 改用搜索定位仓库页与可读文件，不重复相同调用。 |
| 沙箱内 `curl` 无法解析 `api.github.com` | 1 | 按权限流程在用户批准后以只读方式访问 GitHub API，并将源码快照仅解压到 `/private/tmp`。 |
| 阶段 14 首次同时更新计划与进度时，`progress.md` 锚点不在文件末尾 | 1 | 读取两个文件尾部后，按实际末尾分别追加，不重复使用失效锚点。 |
| 根目录 `pytest -q` 收集未跟踪 `reference_code/` 的外部工程测试并产生 29 个环境/网络错误 | 1 | 不修改参考工程；改为运行本仓库受控测试目录 `pytest -q tests`，后续实施 P0 时补充 pytest 收集边界。 |
| 论文包组合补丁预期 README 存在“研究与架构文档”标题，但实际标题为 `Framework Design` | 1 | 组合补丁整体未应用；读取真实锚点后拆成小补丁，不重复原操作。 |
| 论文合同聚焦测试有 2 项过度依赖句子原文和阈值排版 | 1 | 保留科学边界，将测试改为验证关键语义与实际 `10/20/30 mm` 统一表达。 |
| 首次暂存后 `git diff --cached --check` 发现论文 Markdown 的行尾双空格和文件末尾多余空行 | 1 | 使用纯机械格式清理后重新暂存并运行完整检查。 |
| AI4Science 合同聚焦测试有 2 项固定字符串未命中：洪水边界为同义表达，`Causal` 带 Markdown 加粗 | 1 | 在正文增加明确的无洪水推断句，测试改为忽略加粗排版的语义检查。 |
