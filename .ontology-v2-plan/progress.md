# 解释证据图谱 2.0 进度

## 2026-07-31

- 完成本体、SHACL、构图器、文献增强、解释服务和前端手工图交叉审查。
- 确定优先顺序：因果安全门禁 → 状态/环境模型 → 旧图迁移 → 可执行验证。
- 计划目录首次选择 `.codex/plans/ontology-v2/`，因权限只读改至
  `.ontology-v2-plan/`。
- 开始阶段1：修复未经审核边的因果资格。
- 已将结构图生成器、文献合并器、源图和前端内置图中的13条不安全边改为
  `eligible_for_causal_explanation=false`，并增加禁止待审核证据获得因果资格的测试。
- 完成本体2.0状态与环境骨架：新增阈值派生状态、证据环境和时间环境；极端气象状态
  成为指标状态子类；三个灾害有利状态显式连接其筛查气象状态。
- 修复构图器，使普通和极端状态实例都写入 `derivedFromIndicator`。
- 将混合“高程+坡度”的 `Orography` 概念收窄并迁移为 `SurfaceAltitude`。
- 首轮聚焦测试发现旧术语断言和本体概念/实例混合计数问题，已分别修正测试边界。
- 解释接口升级为v3：统一返回灾害有利筛查状态，并单独声明用于检索观测背景的气象
  目标状态，避免把极端气象状态直接当作灾害状态。
- 本体2.0阶段验证通过：Python聚焦测试46项、前端测试10项。
- 新增旧图确定性迁移器：机制统一为本体IRI，灾害绑定灾害有利状态，已覆盖指标绑定
  本体指标IRI，未覆盖概念显式标记为未映射；所有旧边标为非因果兼容视图关系。
- 结构图生成器和文献合并器现会自动执行迁移，避免重新生成后退回双机制词表。
- 旧图迁移与机制对齐聚焦测试35项通过。
- 新增无外部依赖的本体与图实例语义门禁；物化前验证状态/环境/CF属性模型，统计图
  写库前验证指标来源、断言必要边和全部非因果资格。
- 统一重建清单现包含本体语义摘要；语义门禁相关58项测试及Python编译检查通过。
